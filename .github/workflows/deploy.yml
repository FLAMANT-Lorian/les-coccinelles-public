name: Deploy to Forge

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Forge Deploy
        run: curl -f "${{ secrets.FORGE_DEPLOY_HOOK }}"
