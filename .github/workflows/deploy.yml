name: 'Deploy on push'

on:
  push:
    branches:
      - main

jobs:
  forge-deploy:
    name: 'Laravel Forge Deploy'
    runs-on: ubuntu-latest

    steps:
      # Trigger Laravel Forge Deploy
      - name: Debug secret length
        run: echo "${{ secrets.FORGE_DEPLOY_HOOK }}" | wc -c
